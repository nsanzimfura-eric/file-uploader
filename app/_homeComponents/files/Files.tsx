import Form from "../form/Form";

interface FilesProps { }

const Files = async (props: FilesProps) => {
    const { } = props;
    return (<div className='flex-1 m-0 p-0'>
        <Form />
    </div>);
};

export default Files;