interface FeedbackNotificationProps {
    message: string
}

const FeedbackNotification = (props: FeedbackNotificationProps) => {
    const { message } = props;

    return (<div className=''>FeedbackNotification</div>);
};

export default FeedbackNotification;